server:
  port: 80
spring:
  application:
    name: consul-consumer-order
  cloud:
    consul:
      discovery:
        service-name: ${spring.application.name}
      host: 192.168.41.128
      port: 8500